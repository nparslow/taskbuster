
# made with:
# python manage.py dumpdata --indent 2 --natural-foreign -e contenttypes -e auth.Permission > taskbuster/fixtures/allauth_fixture.json
# this contains sensitive info so should be omitted from git
# also all instances of 127.0.0.1:8000  are replaced with localhost:8081